import template from './<%= name %>.html';
import controller from './<%= name %>.controller';
import './<%= name %>.css';

let <%= name %>Component = {
  template,
  controller
};

export default <%= name %>Component;
